let series = require('./series/series.js') // requiero lo que exporta el archivo series.js (array de series)
let seriesView = require('./series/seriesView')


console.log(seriesView(series)) // Game of thrones